<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="public/main.css">
    <link rel="stylesheet" href="public/animate.css">
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.0.4/socket.io.js"></script>
    <!-- <script type="text/javascript" src="public/javaScript/loadUsername.js"></script> -->
    <!-- <script type="text/javascript" src = "public/javaScript/onClick.js"></script> -->
    <script type="text/javascript" src = "public/javaScript/messages.js"></script>
    <title>Sam's chat</title>
  </head>
  <body>
    <div id="top"><span id="samschat">Sam's chat</span>
      <div id="userControlls" class="animated zoomIn">
        <a href="/profile/<%=username%>">Profile</a>
        <a href="/logof">Log of</a>
      </div>
      <div id="search" >
        <input placeholder="Search for user" id="searchField" type="text"/><input id="searchButton" type="button">
      </div>
    </div>
    <main>
      <div id="nav">
        <%
          if(friends.length == 0) {%>
            <p>No friends here yet.</p>
          <%}
          for(let i = 0; i< friends.length; i++)
          {%>
            <div class="friend" id="<%=friends[i].friend%>"><%=friends[i].friend%>
              <%if(friends[i].read == "unread"){%>--new messages--<%}%>
            </div>
          <%}%>
      </div>
      <div id="line"></div>
      <div id="chat">
        <div id="chatEnter">
          <input id="userText" type="text"><input id="sendButton" type="button" value="send"/>
        </div>
        <%for(let i = messages.length - 1; i >= 0; i--){%>
          <p class="message <%if(messages[i].sender == username){%>
            messageYour<%}
            else{%>
            messageOthers
            <%}%>"><%=messages[i].sender%>:<%=messages[i].text%></p>
        <%}%>
      </div>
    </main>
  </body>
</html>
